#include "main.h"
#include <ctype.h>
/**
*int _islower(int c) - Checks for lowercasesi
*
*Return: 0
*On error, 1 is returned, and error is set appropriately.
*
*/
int _islower(int c)
{
c = 'c';

if (_islower(c))
	{
	_putchar(c);
	}
	else
		{
		return (1);
		}
return (0);
}
