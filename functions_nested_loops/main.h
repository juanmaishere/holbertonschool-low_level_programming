#ifndef HEADER_FILE
#define HEADER_FILE
int _putchar(char c);
/**
 *print_alphabet - Prints the lowercase alphabet.
 *This function prints the lowercase alphabet from 'a' to 'z'
 *on a single line.
 */
void print_alphabet(void);
/**
 *print_alphabet_x10 - Prints the lowercase alphabet ten times.
 *
 *This function prints the lowercase alphabet ten times,
 *with each iteration separated by a newline character.
 */
void print_alphabet_x10(void);
 /**
  *    _islower - Function that checks for lowercases
  *    This function finds the lowercase in the alphabet
  */
int _islower(int c);
int _isalpha(int c);
int print_sign(int n);
int _abs(int);
int print_last_digit(int);
void jack_bauer(void);
void times_table(void);
#endif
