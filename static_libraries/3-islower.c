#include <stdio.h>
/**
*int _islower(int c) - prints 10 times the alphabet
*
*Description: Prints 10 times
*Return: Always 0.
*/
int _islower(int c)
{
if (c >= 'a' && c <= 'z')
{
return (1);
}
else
{
return (0);
}
}
